spring.application.name=mytube_users
#API Config
server.port=8081

# Configurar la zona horaria de la JVM
spring.jackson.time-zone=America/Asuncion

# Configuracion de conexion a la base de datos MySQL
spring.datasource.url=jdbc:mysql://distri-db.rodrigomaidana.com:3306/mytube_sd?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Asuncion
spring.datasource.username=jorge-figueredo
spring.datasource.password=d6BAlb59.13THzLx6
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.jdbc.time_zone=America/Asuncion
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Mostrar consultas SQL
spring.jpa.show-sql=true

# Debugging adicional
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.web.FilterChainProxy=TRACE

spring.cache.type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

logging.level.root = info

spring.data.rest.default-page-size=5

#Eureka Server
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true
spring.cloud.compatibility-verifier.enabled=false

# Intervalo de renovacion de la instancia
eureka.instance.leaseRenewalIntervalInSeconds=300
# Tiempo de espera antes de que Eureka considere una instancia como inactiva
eureka.instance.leaseExpirationDurationInSeconds=600

# Tiempo de espera antes de que Eureka considere una instancia como inactiva
#eureka.instance.leaseExpirationDurationInSeconds=600